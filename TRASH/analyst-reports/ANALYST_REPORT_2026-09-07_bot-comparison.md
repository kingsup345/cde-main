# דוח אנליסט — סתירות ובאגים בדף "השוואת ביצועי הבוטים"

**תאריך:** 2026-09-07
**קובץ תחת בדיקה:** `src/pages/BacktestResults.tsx`
**טריגר:** בתמונה שסופקה, "רווח/הפסד כולל" מציג **−90%** לכל ארבעת הבוטים, בעוד ה-PnL הממומש מהעסקאות הוא סנטים בודדים.
**מסקנה בשורה אחת:** ה-−90% הוא **ארטיפקט חישובי**, לא תוצאה אמיתית. הדף מודד מול בסיס הון שגוי וקבוע (‎$10,000‎) ולא מול ההון האמיתי של כל בוט. **אי אפשר לבחור "בוט אמיתי" מהמסך הזה במצבו הנוכחי.**

---

## תקציר מנהלים

הדף מערבב שני מדדים שונים של רווח/הפסד תחת אותה כותרת, ואחד מהם מחושב מול קבוע קשיח.
כל בוט רץ עם הון התחלתי משלו (`config.initialAmount` — זו כל מהות ההפרדה בין הבוטים). הדף מתעלם מזה
ומחשב `equity − 10,000` לכל בוט. אם בוט רץ על ‎$1,000‎ הון, התוצאה היא תמיד
`1,000 − 10,000 = −9,000` ‏≈ ‏**−90%**, בלי קשר למה שהבוט באמת עשה.

זה בדיוק מה שרואים בתמונה: ארבעה בוטים, ארבע פעמים ‏−90%, כי לארבעתם הוגדר הון נמוך מ-‎$10,000‎.

---

## ממצאים לפי חומרה

### 🔴 F-1 — בסיס ההון מקודד קשיח ל-‎$10,000‎ (שורש ה-−90%)

**מיקום:** `src/pages/BacktestResults.tsx:42`, `:56`, `:63`

```ts
const START_CAPITAL = 10_000;
...
const pnlTotalUsd = equity - START_CAPITAL;
pnlTotalPct: (pnlTotalUsd / START_CAPITAL) * 100,
```

**המנגנון:** `statsFor()` מקבל `equity` אבל לא מקבל את `config.initialAmount` של הבוט.
כל בוט ב-`SIM_BOTS` מחזיק הון נפרד (`SIM_BASE_DEFAULTS.initialAmount` הוא רק ברירת מחדל;
המשתמש משנה אותו לכל בוט בנפרד — ראה גם פרק ה-"‎$80 < $100‎" בבוט פרו, ששם ההון היה ~‎$800‎).

**הדגמה מספרית (תואם לתמונה):**

| בוט | הון אמיתי (`initialAmount`) | `equity` בפועל | מה הדף מציג | מה נכון |
|---|---|---|---|---|
| מגמו חדש | ~$1,000 | ~$1,000 | `−$9,000 (−90.0%)` | `≈ $0 (≈ 0%)` |
| פרו | ~$1,000 | ~$1,000 | `−$9,002 (−90.0%)` | `≈ $0 (≈ 0%)` |
| נתיב 4H | ~$1,000 | ~$1,000 | `−$9,000 (−90.0%)` | `≈ +$1 (≈ +0.1%)` |
| Bybit | ~$1,000 | ~$999 | `−$9,001 (−90.0%)` | `≈ −$1 (≈ −0.1%)` |

**תיקון:**
```ts
function statsFor(key, label, running, equity, base, dd, trades) {   // + base
  ...
  const pnlTotalUsd = equity - base;
  pnlTotalPct: base ? (pnlTotalUsd / base) * 100 : 0,
}
// בקריאה:
statsFor(b.key, b.label, b.ctx.isRunning, b.ctx.equity,
         b.ctx.config.initialAmount || START_CAPITAL,          // ← ההון האמיתי
         b.ctx.dailyDrawdownPercent, ...)
```

---

### 🔴 F-2 — שורת ה-"סה"כ" סובלת מאותו באג, מוכפל פי 4

**מיקום:** `src/pages/BacktestResults.tsx:124`

```ts
pnlUsd: equity - START_CAPITAL * bots.length,   // equity - 40,000
```

עם ‎$1,000‎ לבוט: `4,000 − 40,000 = −36,000`. צריך להיות `Σ equityᵢ − Σ initialAmountᵢ`.

**תיקון:**
```ts
const base = bots.reduce((s, b) => s + (b.ctx.config.initialAmount || 0), 0);
...
pnlUsd: equity - base,
```

---

### 🟠 F-3 — שני מדדי P&L שונים מוצגים זה לצד זה בלי הבחנה

**מיקום:** טבלת "השוואה בין הבוטים", עמודות "רווח/הפסד כולל" מול "PnL ממומש" (`:327` מול `:336`)

- `pnlTotalUsd = equity − base` — **Mark-to-Market**: כולל רווח/הפסד לא-ממומש על פוזיציות פתוחות, **וגם** עמלות, סליפג' ופאנדינג מצטברים.
- `realizedPnl = Σ pnl` — **רק עסקאות סגורות**.

השניים כמעט לעולם לא יתלכדו. המשתמש רואה "−$9,000 כולל" ליד "−$0.19 ממומש" ומסיק שיש סתירה —
בעוד שגם אחרי תיקון F-1 עדיין תהיה פער אמיתי (unrealized + עלויות). צריך:
1. לשנות כותרת ל-"שינוי הון (MtM)" מול "רווח ממומש".
2. להוסיף עמודת ביניים: "לא ממומש" = `equity − base − realizedPnl` (אם אין דרך ישירה) או `Σ unrealizedPnl` מהפוזיציות.

---

### 🟠 F-4 — `hasServerData` לא נבדק: בוט מנותק נספר כ-"שטוח, 0%"

**מיקום:** `BacktestResults.tsx` — כל השימושים ב-`path.equity` / `bybit.equity`

הקונטקסטים של נתיב 4H ו-Bybit הם **שרת-בלבד**. כשה-Worker לא זמין הם מחזירים
`EMPTY_SNAPSHOT` עם `equity: 10000` ו-`hasServerData: false`
(‏`PathSimulationBotContext.tsx:89-95, 236`‏).

הדף לא בודק את הדגל. תוצאה: Worker נופל ⟶ נתיב 4H ו-Bybit מציגים פתאום `equity 10,000`,
`pnlTotal 0`, `0%` — שנראה כמו תוצאה אמיתית ("הבוט שטוח") ומזהם את ההשוואה ואת שורת ה-"סה"כ".

**סתירה פנימית באותו קוד-בייס:** בדף `SimulationBot.tsx` הפונקציה `combineRisk()`
**כן** מוציאה בוטים עם `!dataAvailable` מכל חישוב (`botAggregation.ts:144`, `unavailableEngines`).
שני חלקי ה-UI מתייחסים לאותו מצב הפוך.

**תיקון:** לסנן `bots` ל-`b.ctx.hasServerData !== false` לפני הסטטיסטיקה, ולהציג "אין נתוני שרת" מפורש לבוט מנותק.

---

### 🟡 F-5 — הדף מחשב מחדש Win Rate / מספר עסקאות במקום להשתמש בנתוני השרת

**מיקום:** `BacktestResults.tsx:50-73` (`statsFor`) מול ה-snapshot שכבר נושא `winRate` ו-`closedTrades`

השרת מחזיר `winRate`, `closedTrades`, `totalTrades` מחושבים במנוע. הדף מתעלם מהם ובונה משלו מ-`trades[]`, עם שתי הגדרות שונות:

1. **`partial_tp1` נספר כעסקה סגורה נפרדת** — פוזיציה אחת שיוצאת ב-2 שלבים (scale-out)
   נספרת כ-2 "עסקאות סגורות". זה מנפח את `closedTrades` ומעוות את ה-Win Rate.
   (רלוונטי במיוחד ל-Bybit/TrendBreakout עם scale-in 50/30/20.)
2. **עסקת break-even (`pnl === 0`) מסווגת כהפסד** — `losses = filter(pnl <= 0)` (`:53`).

תוצאה: ה-"Win Rate 40%" בדף הזה יכול לסתור את ה-"הצלחה X%" של אותו בוט בעמודה שלו ב-`/simulation-bot`,
כי אחד server-authoritative והשני client-derived.

**תיקון:** להעדיף `b.ctx.winRate` / `b.ctx.closedTrades` מהקונטקסט; אם בכל זאת מחשבים מקומית —
לאחד `partial_tp1` לפי `positionId`, ולסווג `pnl === 0` בנפרד מ-"הפסד".

---

### 🟡 F-6 — אין בדיקת-שפיות (reconciliation) על המסך

אנליסט שמסתכל על הדף הזה מצפה למצוא בו את הבדיקה:
`|pnlTotalUsd − (realizedPnl + unrealizedPnl − costs)| < ε`.
כרגע אין שום דבר שמסמן כשהמספרים לא סוגרים — וזה בדיוק מה שאיפשר ל-−90% לעבור בשקט.

**המלצה:** שורת אזהרה בראש הטבלה כשהפער בין "שינוי הון" ל-"ממומש + לא-ממומש − עלויות"
גדול מ-1% מההון.

---

## מה שצריך אימות מול ה-Worker החי (לא ניתן לאשר מהקוד לבד)

הרץ במכשיר עם ה-Worker מחובר:

```
GET  ${baseUrl}/api/bybit-sim/state
```

והשווה:

| שדה | ציפייה אם F-1 הוא ההסבר המלא |
|---|---|
| `config.initialAmount` | ‎~1,000‎ (או כל ערך ≠ 10,000) |
| `snapshot.equity` | קרוב מאוד ל-`config.initialAmount` |
| `snapshot.closedTrades` | 1–5 |
| `Σ snapshot.trades[].pnl` | סנטים בודדים |

אם `initialAmount` הוא **10,000** ובכל זאת `equity ≈ 1,000` — יש באג שני, עמוק יותר, בחישוב
ה-equity במנוע עצמו (`server/simEngineFactory.ts` — mark-to-market / funding), וה-−90% אמיתי חלקית.
הנתונים הזמינים (PnL ממומש בסנטים, רצפת פוזיציה ‎$100‎, 1–5 עסקאות) **לא תומכים** בתרחיש הזה,
אבל צריך לשלול אותו בעיניים.

---

## סדר תיקון מומלץ

1. **F-1 + F-2** — להעביר `initialAmount` אמיתי ל-`statsFor` ולשורת הסה"כ. מתקן את ה-−90% מיידית.
2. **F-4** — לסנן בוטים מנותקים. ‏(עקביות עם `combineRisk`.)‏
3. **F-3 + F-5** — כותרות ברורות + להעדיף נתוני שרת. מסיר את שאר הסתירות הנראות למשתמש.
4. **F-6** — שורת reconciliation כרשת ביטחון.

אף אחד מהתיקונים **לא נוגע באלגוריתם או במנוע** — הכל בשכבת התצוגה של `BacktestResults.tsx`.
נדרש רק re-deploy של ה-frontend (Netlify), לא של ה-Worker.

---

## סטטוס תיקון — יושם 2026-09-07

כל F-1…F-6 תוקנו ב-`src/pages/BacktestResults.tsx` (שכבת תצוגה בלבד):

| # | מה נעשה |
|---|---|
| F-1 | `statsFor` מקבל `base = ctx.config.initialAmount` (fallback ‎10,000‎); `pnlTotalUsd = equity − base`, `pnlTotalPct` מנורמל מולו. |
| F-2 | שורת סה"כ: `Σ equityᵢ − Σ baseᵢ`, רק על בוטים עם נתונים. |
| F-3 | עמודות: "שינוי הון (MtM)" · "לא ממומש" (חדשה, מחושבת עצמאית מהפוזיציות) · "רווח ממומש". הסבר-על מתחת לכותרת. |
| F-4 | `hasData = 'hasServerData' in ctx ? ctx.hasServerData !== false : true`. בוט מנותק ⟶ "אין נתוני שרת", מוחרג מהסה"כ, מצוין בהערת שוליים. |
| F-5 | Win Rate ומספר עסקאות מ-`ctx.winRate` / `ctx.closedTrades` (server-authoritative). break-even (`pnl === 0`) בעמודת "תיקו" נפרדת, לא כהפסד. |
| F-6 | בדיקת reconciliation: באנר אדום כש-`|MtM − (ממומש + לא-ממומש)| > max(1% מההון, $5)`, עם פירוט לכל בוט. |

typecheck נקי · 311 טסטים עוברים · lint נקי. דורש rebuild של Netlify בלבד.

---

## הערכת אנליסט

עד לתיקון F-1, טבלת ההשוואה **חסרת ערך להחלטה** — היא מודדת "כמה ההון קטן מ-‎$10k‎",
לא את איכות האסטרטגיה. אחרי התיקון, עם 1–5 עסקאות סגורות לבוט ו-PnL ברמת הסנט,
עדיין **לא יהיה מדגם** לבחור ממנו בוט אמיתי. צריך לצבור עשרות עסקאות סגורות לכל בוט,
ולהשוות על בסיס מנורמל (R-multiple או תשואה על `initialAmount`), לא בדולרים גולמיים
כשההון שונה בין הבוטים.
